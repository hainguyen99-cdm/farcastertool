# Database Configuration
MONGODB_URI=mongodb://localhost/farcaster-tool

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Server Configuration
PORT=3002
NODE_ENV=development

# Encryption Key (generate a secure key for production)
ENCRYPTION_KEY=your-32-character-encryption-key-here

# Farcaster API Configuration (if needed)
FARCASTER_API_URL=https://client.farcaster.xyz

# Game API credentials
# Public API key
RPC_VERSION_API_KEY=
# Secret used to sign payload
RPC_VERSION_SECRET=
# Legacy alternate secret key name (supported by code)
RPC_VERSION_SECRET_KEY=