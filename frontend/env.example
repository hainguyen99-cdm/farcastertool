# Frontend Configuration
# Server-side only - not exposed to client to prevent CORS issues
BACKEND_URL=http://localhost:3002
# Nginx proxy URL for CORS handling (if using nginx)
NGINX_PROXY_URL=http://localhost:3000

# Environment
NODE_ENV=development